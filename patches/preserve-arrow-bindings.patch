diff --git a/packages/element/src/binding.ts b/packages/element/src/binding.ts
index ede907972..62237d7d2 100644
--- a/packages/element/src/binding.ts
+++ b/packages/element/src/binding.ts
@@ -872,20 +872,25 @@ const getBindingStrategyForDraggingBindingElementEndpoints_simple = (
                 : globalPoint,
               hit,
               startDragged ? "start" : "end",
               elementsMap,
               appState.zoom,
               appState.isMidpointSnappingEnabled &&
                 !opts?.angleLocked &&
                 !appState.gridModeEnabled,
             ) || globalPoint,
         }
+    : // Preserve existing binding if no hit found (prevents accidental unbind during edit)
+      startDragged && arrow.startBinding?.elementId
+    ? { mode: undefined }
+    : endDragged && arrow.endBinding?.elementId
+    ? { mode: undefined }
     : { mode: null };
 
   const otherEndpoint = LinearElementEditor.getPointAtIndexGlobalCoordinates(
     arrow,
     startDragged ? -1 : 0,
     elementsMap,
   );
   const pointIsCloseToOtherElement =
     otherFocusPoint &&
     otherBindableElement &&
