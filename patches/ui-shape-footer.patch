diff --git a/packages/common/src/constants.ts b/packages/common/src/constants.ts
index b0e351b9e..c4f588a31 100644
--- a/packages/common/src/constants.ts
+++ b/packages/common/src/constants.ts
@@ -106,6 +106,8 @@ export const CLASSES = {
   CONVERT_ELEMENT_TYPE_POPUP: "ConvertElementTypePopup",
   SHAPE_ACTIONS_THEME_SCOPE: "shape-actions-theme-scope",
   FRAME_NAME: "frame-name",
+  SHAPE_CHROME: "excalidraw__shape-chrome",
+  SHAPE_FOOTER: "excalidraw__shape-footer",
   DROPDOWN_MENU_EVENT_WRAPPER: "dropdown-menu-event-wrapper",
 };
 
@@ -216,6 +218,40 @@ export const FRAME_TITLE_BLOCK_HEIGHT =
   FRAME_STYLE.nameInsetY +
   FRAME_STYLE.nameFontSize * FRAME_STYLE.nameLineHeight;
 
+export const SHAPE_FOOTER_PADDING = {
+  top: 16,
+  horizontal: 16,
+  bottom: 12,
+} as const;
+
+/** Default single-line footer label height (scene units). */
+export const SHAPE_FOOTER_CONTENT_HEIGHT = 16;
+
+/** Space reserved at the bottom of a shape/frame so children stay above the footer. */
+export const SHAPE_FOOTER_RESERVED_HEIGHT =
+  SHAPE_FOOTER_PADDING.top +
+  SHAPE_FOOTER_CONTENT_HEIGHT +
+  SHAPE_FOOTER_PADDING.bottom;
+
+/** Insets for top-left / top-right chrome (matches the in-frame title). */
+export const SHAPE_TOP_PADDING = {
+  top: 12,
+  horizontal: 16,
+  bottom: 12,
+} as const;
+
+/** Gap between a top-left slot and the native frame name. */
+export const SHAPE_TOP_TITLE_GAP = 8;
+
+/** Stacked top-right chrome (e.g. count + collapse icon). */
+export const SHAPE_TOP_CONTENT_MIN_HEIGHT = 36;
+
+/** Space reserved at the top of a frame so children stay below title / top slots. */
+export const SHAPE_TOP_RESERVED_HEIGHT =
+  SHAPE_TOP_PADDING.top +
+  SHAPE_TOP_CONTENT_MIN_HEIGHT +
+  SHAPE_TOP_PADDING.bottom;
+
 export const MIN_FONT_SIZE = 1;
 export const DEFAULT_FONT_SIZE = 20;
 export const DEFAULT_FONT_FAMILY: FontFamilyValues = FONT_FAMILY.Excalifont;
diff --git a/packages/element/src/frame.ts b/packages/element/src/frame.ts
index 8d7f754b5..de10b277e 100644
--- a/packages/element/src/frame.ts
+++ b/packages/element/src/frame.ts
@@ -2,6 +2,8 @@ import {
   arrayToMap,
   FRAME_STYLE,
   FRAME_TITLE_BLOCK_HEIGHT,
+  SHAPE_FOOTER_RESERVED_HEIGHT,
+  SHAPE_TOP_RESERVED_HEIGHT,
 } from "@excalidraw/common";
 import {
   isPointWithinBounds,
@@ -153,8 +155,12 @@ const alignmentOffset = (align: "start" | "center" | "end", extra: number) => {
   return 0;
 };
 // Title block height (nameInsetY 12 + nameFontSize * nameLineHeight 17.5 = 29.5).
-// The 12px gap below the title is padding.vertical, not part of this constant.
-export const DEFAULT_FRAME_TITLE_HEIGHT = FRAME_TITLE_BLOCK_HEIGHT;
+// Raised to SHAPE_TOP_RESERVED_HEIGHT when top-left/top-right chrome is taller
+// (e.g. stacked count + collapse). The 12px gap below is padding.vertical.
+export const DEFAULT_FRAME_TITLE_HEIGHT = Math.max(
+  FRAME_TITLE_BLOCK_HEIGHT,
+  SHAPE_TOP_RESERVED_HEIGHT,
+);
 
 export const getFrameNameFontSize = (
   frame?: Pick<ExcalidrawFrameLikeElement, "fontSize"> | null,
@@ -178,9 +184,12 @@ export const getFrameNameColor = (
 
 export const getFrameTitleReservedHeight = (
   frame?: Pick<ExcalidrawFrameLikeElement, "fontSize"> | null,
-) =>
-  FRAME_STYLE.nameInsetY +
-  getFrameNameFontSize(frame) * FRAME_STYLE.nameLineHeight;
+) => {
+  const titleHeight =
+    FRAME_STYLE.nameInsetY +
+    getFrameNameFontSize(frame) * FRAME_STYLE.nameLineHeight;
+  return Math.max(titleHeight, SHAPE_TOP_RESERVED_HEIGHT);
+};
 
 export const isFrameCollapsed = (
   frame?: Pick<ExcalidrawFrameLikeElement, "collapsed"> | null,
@@ -206,7 +215,11 @@ export const getFittedFrameGeometry = (
     x: x1 - padX,
     y: y1 - titleHeight - padY,
     width: x2 - x1 + padX * 2,
-    height: y2 - y1 + titleHeight + padY * 2,
+    height:
+      y2 - y1 +
+      titleHeight +
+      padY +
+      SHAPE_FOOTER_RESERVED_HEIGHT,
   };
 };
 
@@ -1060,7 +1073,7 @@ export const layoutFrameChildren = (
   }
 
   const titleHeight = getFrameTitleReservedHeight(frame);
-  const bottomReserve = padding.vertical;
+  const bottomReserve = SHAPE_FOOTER_RESERVED_HEIGHT;
   const contentFrameWidth = contentWidth + padding.horizontal * 2;
   const contentFrameHeight =
     contentHeight + titleHeight + padding.vertical + bottomReserve;
diff --git a/packages/excalidraw/components/App.tsx b/packages/excalidraw/components/App.tsx
index 29e4f7c20..239ebd59a 100644
--- a/packages/excalidraw/components/App.tsx
+++ b/packages/excalidraw/components/App.tsx
@@ -36,6 +36,10 @@ import {
   ELEMENT_TRANSLATE_AMOUNT,
   EVENT,
   FRAME_STYLE,
+  SHAPE_FOOTER_PADDING,
+  SHAPE_TOP_CONTENT_MIN_HEIGHT,
+  SHAPE_TOP_PADDING,
+  SHAPE_TOP_TITLE_GAP,
   IMAGE_MIME_TYPES,
   IMAGE_RENDER_TIMEOUT,
   LINE_CONFIRM_THRESHOLD,
@@ -184,6 +187,8 @@ import {
   getElementsInResizingFrame,
   getElementsInNewFrame,
   getContainingFrame,
+  getAncestorFrames,
+  shouldApplyFrameClip,
   elementOverlapsWithFrame,
   updateFrameMembershipOfSelectedElements,
   isElementInFrame,
@@ -269,6 +274,7 @@ import {
   inferFrameAutoLayoutDirection,
   getFrameNameColor,
   getFrameNameFontSize,
+  getFrameNameTextAlign,
   getCollapsedFrameHeight,
   getAutoLayoutDropIndicator,
   positionAutoLayoutDrop,
@@ -688,6 +694,16 @@ class App extends React.Component<AppProps, AppState> {
   public files: BinaryFiles = {};
   public imageCache: AppClassProperties["imageCache"] = new Map();
   private iFrameRefs = new Map<ExcalidrawElement["id"], HTMLIFrameElement>();
+  private shapeChromeSlotWidths = new Map<
+    string,
+    { topLeft: number; topRight: number }
+  >();
+  private shapeChromeObservers = new Map<string, ResizeObserver>();
+  private shapeChromeObservedNodes = new Map<string, HTMLDivElement>();
+  private shapeChromeRefCallbacks = new Map<
+    string,
+    (node: HTMLDivElement | null) => void
+  >();
   /**
    * Indicates whether the embeddable's url has been validated for rendering.
    * If value not set, indicates that the validation is pending.
@@ -2198,14 +2214,12 @@ class App extends React.Component<AppProps, AppState> {
         return null;
       }
 
-      const [titleSceneX, titleSceneY] = pointRotateRads(
-        pointFrom(f.x + FRAME_STYLE.nameInsetX, f.y + FRAME_STYLE.nameInsetY),
-        pointFrom(f.x + f.width / 2, f.y + f.height / 2),
-        f.angle,
-      );
-      const { x: x1, y: y1 } = sceneCoordsToViewportCoords(
-        { sceneX: titleSceneX, sceneY: titleSceneY },
-        this.state,
+      const { topLeftWidth, topRightWidth, titleGap } =
+        this.getFrameNameChromeOffset(f);
+      const { x: x1, y: y1 } = this.getFrameNameViewportOrigin(
+        f,
+        topLeftWidth,
+        titleGap,
       );
 
       const FRAME_NAME_EDIT_PADDING = 6;
@@ -2252,6 +2266,10 @@ class App extends React.Component<AppProps, AppState> {
               transform: `translate(-${FRAME_NAME_EDIT_PADDING}px, ${FRAME_NAME_EDIT_PADDING}px)`,
               color: getFrameNameColor(f, isDarkTheme),
               overflow: "hidden",
+              width: "100%",
+              textAlign: getFrameNameTextAlign(
+                f,
+              ) as React.CSSProperties["textAlign"],
               maxWidth: `${
                 this.ownerDocument.body.clientWidth -
                 x1 -
@@ -2276,22 +2294,44 @@ class App extends React.Component<AppProps, AppState> {
           key={f.id}
           style={{
             position: "absolute",
-            // Position the frame name inside the frame at top-left
+            // Position the frame name inside the frame at top-left,
+            // shifted right when a top-left chrome slot is present.
             top: `${y1 - this.state.offsetTop}px`,
             left: `${x1 - this.state.offsetLeft}px`,
             transform: `rotate(${f.angle}rad)`,
             transformOrigin: "top left",
             zIndex: 2,
+            display: "flex",
+            alignItems: "center",
+            height: `${SHAPE_TOP_CONTENT_MIN_HEIGHT * this.state.zoom.value}px`,
             fontSize: getFrameNameFontSize(f) * this.state.zoom.value,
             color: getFrameNameColor(f, isDarkTheme),
             lineHeight: FRAME_STYLE.nameLineHeight,
-            width: "max-content",
+            width: `${
+              Math.max(
+                0,
+                f.width -
+                  FRAME_STYLE.nameInsetX * 2 -
+                  topLeftWidth -
+                  titleGap -
+                  topRightWidth,
+              ) * this.state.zoom.value
+            }px`,
+            textAlign: getFrameNameTextAlign(
+              f,
+            ) as React.CSSProperties["textAlign"],
             maxWidth:
               focusedSearchMatch?.id === f.id && focusedSearchMatch?.focus
                 ? "none"
                 : `${
-                    Math.max(0, f.width - FRAME_STYLE.nameInsetX * 2) *
-                    this.state.zoom.value
+                    Math.max(
+                      0,
+                      f.width -
+                        FRAME_STYLE.nameInsetX * 2 -
+                        topLeftWidth -
+                        titleGap -
+                        topRightWidth,
+                    ) * this.state.zoom.value
                   }px`,
             overflow: f.id === this.state.editingFrame ? "visible" : "hidden",
             whiteSpace: "nowrap",
@@ -2316,6 +2375,347 @@ class App extends React.Component<AppProps, AppState> {
     });
   };
 
+  private getFrameNameChromeOffset = (frame: ExcalidrawFrameLikeElement) => {
+    const chromeSlots = this.shapeChromeSlotWidths.get(frame.id);
+    const topLeftWidth = chromeSlots?.topLeft ?? 0;
+    const topRightWidth = chromeSlots?.topRight ?? 0;
+    const titleGap = topLeftWidth > 0 ? SHAPE_TOP_TITLE_GAP : 0;
+    return { topLeftWidth, topRightWidth, titleGap };
+  };
+
+  private getFrameNameViewportOrigin = (
+    frame: ExcalidrawFrameLikeElement,
+    topLeftWidth: number,
+    titleGap: number,
+  ) => {
+    const [titleSceneX, titleSceneY] = pointRotateRads(
+      pointFrom(
+        frame.x + FRAME_STYLE.nameInsetX + topLeftWidth + titleGap,
+        frame.y + FRAME_STYLE.nameInsetY,
+      ),
+      pointFrom(frame.x + frame.width / 2, frame.y + frame.height / 2),
+      frame.angle,
+    );
+    return sceneCoordsToViewportCoords(
+      { sceneX: titleSceneX, sceneY: titleSceneY },
+      this.state,
+    );
+  };
+
+  private setShapeChromeSlotWidth = (
+    elementId: string,
+    slot: "topLeft" | "topRight",
+    width: number,
+  ) => {
+    const prev = this.shapeChromeSlotWidths.get(elementId) ?? {
+      topLeft: 0,
+      topRight: 0,
+    };
+    if (prev[slot] === width) {
+      return false;
+    }
+    this.shapeChromeSlotWidths.set(elementId, {
+      ...prev,
+      [slot]: width,
+    });
+    return true;
+  };
+
+  private syncFrameNameChromeOffset = (elementId: string) => {
+    const element = this.scene.getElement(elementId);
+    if (!element || !isFrameLikeElement(element)) {
+      return;
+    }
+    const titleEl = document.getElementById(this.getFrameNameDOMId(element));
+    if (!(titleEl instanceof HTMLElement)) {
+      return;
+    }
+
+    const { topLeftWidth, topRightWidth, titleGap } =
+      this.getFrameNameChromeOffset(element);
+    const { x: x1, y: y1 } = this.getFrameNameViewportOrigin(
+      element,
+      topLeftWidth,
+      titleGap,
+    );
+
+    titleEl.style.top = `${y1 - this.state.offsetTop}px`;
+    titleEl.style.left = `${x1 - this.state.offsetLeft}px`;
+    titleEl.style.transform = `rotate(${element.angle}rad)`;
+    titleEl.style.transformOrigin = "top left";
+    titleEl.style.display = "flex";
+    titleEl.style.alignItems = "center";
+    titleEl.style.height = `${SHAPE_TOP_CONTENT_MIN_HEIGHT * this.state.zoom.value}px`;
+    titleEl.style.width = `${
+      Math.max(
+        0,
+        element.width -
+          FRAME_STYLE.nameInsetX * 2 -
+          topLeftWidth -
+          titleGap -
+          topRightWidth,
+      ) * this.state.zoom.value
+    }px`;
+    titleEl.style.maxWidth = `${
+      Math.max(
+        0,
+        element.width -
+          FRAME_STYLE.nameInsetX * 2 -
+          topLeftWidth -
+          titleGap -
+          topRightWidth,
+      ) * this.state.zoom.value
+    }px`;
+  };
+
+  private applyShapeChromeSlotWidth = (
+    elementId: string,
+    slot: "topLeft" | "topRight",
+    width: number,
+  ) => {
+    if (this.setShapeChromeSlotWidth(elementId, slot, Math.round(width))) {
+      this.syncFrameNameChromeOffset(elementId);
+    }
+  };
+
+  private getShapeChromeSlotRef = (
+    elementId: string,
+    slot: "topLeft" | "topRight",
+  ) => {
+    const observerKey = `${elementId}:${slot}`;
+    let callback = this.shapeChromeRefCallbacks.get(observerKey);
+    if (!callback) {
+      callback = (node) => this.bindShapeChromeSlot(elementId, slot, node);
+      this.shapeChromeRefCallbacks.set(observerKey, callback);
+    }
+    return callback;
+  };
+
+  private bindShapeChromeSlot = (
+    elementId: string,
+    slot: "topLeft" | "topRight",
+    node: HTMLDivElement | null,
+  ) => {
+    const observerKey = `${elementId}:${slot}`;
+
+    if (!node) {
+      this.shapeChromeObservers.get(observerKey)?.disconnect();
+      this.shapeChromeObservers.delete(observerKey);
+      this.shapeChromeObservedNodes.delete(observerKey);
+      this.applyShapeChromeSlotWidth(elementId, slot, 0);
+      return;
+    }
+
+    if (this.shapeChromeObservedNodes.get(observerKey) === node) {
+      return;
+    }
+
+    this.shapeChromeObservers.get(observerKey)?.disconnect();
+    this.shapeChromeObservers.delete(observerKey);
+    this.shapeChromeObservedNodes.set(observerKey, node);
+    this.applyShapeChromeSlotWidth(elementId, slot, node.offsetWidth);
+
+    if (typeof this.ownerWindow.ResizeObserver === "function") {
+      const observer = new this.ownerWindow.ResizeObserver(() => {
+        this.applyShapeChromeSlotWidth(elementId, slot, node.offsetWidth);
+      });
+      observer.observe(node);
+      this.shapeChromeObservers.set(observerKey, observer);
+    }
+  };
+
+  private getShapeChromeFrameClipPaths(
+    element: ExcalidrawElement,
+    elementsMap: NonDeletedSceneElementsMap,
+  ): string[] {
+    if (!this.state.frameRendering.enabled) {
+      return [];
+    }
+
+    const clipPaths: string[] = [];
+    for (const frame of [...getAncestorFrames(element, elementsMap)].reverse()) {
+      if (!shouldApplyFrameClip(element, frame, this.state, elementsMap)) {
+        continue;
+      }
+
+      const center = pointFrom(
+        frame.x + frame.width / 2,
+        frame.y + frame.height / 2,
+      );
+      const points = [
+        pointFrom(frame.x, frame.y),
+        pointFrom(frame.x + frame.width, frame.y),
+        pointFrom(frame.x + frame.width, frame.y + frame.height),
+        pointFrom(frame.x, frame.y + frame.height),
+      ]
+        .map((corner) => pointRotateRads(corner, center, frame.angle))
+        .map(
+          ([sceneX, sceneY]) =>
+            `${sceneX - element.x}px ${sceneY - element.y}px`,
+        );
+      clipPaths.push(`polygon(${points.join(", ")})`);
+    }
+    return clipPaths;
+  }
+
+  private renderShapeChrome() {
+    const { renderShapeFooter, renderShapeTopLeft, renderShapeTopRight } =
+      this.props;
+    if (!renderShapeFooter && !renderShapeTopLeft && !renderShapeTopRight) {
+      return null;
+    }
+
+    const scale = this.state.zoom.value;
+    const elementsMap = this.scene.getNonDeletedElementsMap();
+    return this.scene.getNonDeletedElements().map((element) => {
+      if (
+        !(
+          isFrameLikeElement(element) ||
+          element.type === "rectangle" ||
+          element.type === "diamond" ||
+          element.type === "ellipse"
+        )
+      ) {
+        return null;
+      }
+
+      if (
+        !isElementInViewport(
+          element,
+          this.canvas.width / window.devicePixelRatio,
+          this.canvas.height / window.devicePixelRatio,
+          {
+            offsetLeft: this.state.offsetLeft,
+            offsetTop: this.state.offsetTop,
+            scrollX: this.state.scrollX,
+            scrollY: this.state.scrollY,
+            zoom: this.state.zoom,
+          },
+          elementsMap,
+        )
+      ) {
+        return null;
+      }
+
+      const footer = renderShapeFooter?.(element, this.state);
+      const topLeft = renderShapeTopLeft?.(element, this.state);
+      const topRight = renderShapeTopRight?.(element, this.state);
+      const hideFooter =
+        isFrameLikeElement(element) && Boolean(element.collapsed);
+      const hasFooter = !hideFooter && footer != null && footer !== false;
+      const hasTopLeft = topLeft != null && topLeft !== false;
+      const hasTopRight = topRight != null && topRight !== false;
+      if (!hasFooter && !hasTopLeft && !hasTopRight) {
+        return null;
+      }
+
+      const { x, y } = sceneCoordsToViewportCoords(
+        { sceneX: element.x, sceneY: element.y },
+        this.state,
+      );
+
+      const inner = (
+        <div
+          className={`${CLASSES.SHAPE_CHROME}__inner`}
+          style={{
+            position: "relative",
+            width: element.width,
+            height: element.height,
+            transform: `rotate(${element.angle}rad)`,
+            overflow: "hidden",
+          }}
+        >
+          {hasTopLeft && (
+            <div
+              className={`${CLASSES.SHAPE_CHROME}__top-left`}
+              style={{
+                position: "absolute",
+                top: 0,
+                left: 0,
+                height: `${SHAPE_TOP_PADDING.top + SHAPE_TOP_CONTENT_MIN_HEIGHT + SHAPE_TOP_PADDING.bottom}px`,
+                padding: `0 0 0 ${SHAPE_TOP_PADDING.horizontal}px`,
+                boxSizing: "border-box",
+                display: "flex",
+                alignItems: "center",
+                pointerEvents: POINTER_EVENTS.enabled,
+              }}
+            >
+              <div ref={this.getShapeChromeSlotRef(element.id, "topLeft")}>
+                {topLeft}
+              </div>
+            </div>
+          )}
+          {hasTopRight && (
+            <div
+              className={`${CLASSES.SHAPE_CHROME}__top-right`}
+              style={{
+                position: "absolute",
+                top: 0,
+                right: 0,
+                height: `${SHAPE_TOP_PADDING.top + SHAPE_TOP_CONTENT_MIN_HEIGHT + SHAPE_TOP_PADDING.bottom}px`,
+                padding: `0 ${SHAPE_TOP_PADDING.horizontal}px 0 0`,
+                boxSizing: "border-box",
+                display: "flex",
+                alignItems: "center",
+                justifyContent: "flex-end",
+                pointerEvents: POINTER_EVENTS.enabled,
+              }}
+            >
+              <div ref={this.getShapeChromeSlotRef(element.id, "topRight")}>
+                {topRight}
+              </div>
+            </div>
+          )}
+          {hasFooter && (
+            <div
+              className={`${CLASSES.SHAPE_FOOTER} ${CLASSES.SHAPE_FOOTER}__content`}
+              style={{
+                position: "absolute",
+                left: 0,
+                right: 0,
+                bottom: 0,
+                display: "flex",
+                justifyContent: "center",
+                padding: `${SHAPE_FOOTER_PADDING.top}px ${SHAPE_FOOTER_PADDING.horizontal}px ${SHAPE_FOOTER_PADDING.bottom}px`,
+                boxSizing: "border-box",
+              }}
+            >
+              {footer}
+            </div>
+          )}
+        </div>
+      );
+
+      const clippedInner = this.getShapeChromeFrameClipPaths(
+        element,
+        elementsMap,
+      ).reduce(
+        (child, clipPath) => (
+          <div style={{ clipPath, position: "relative" }}>{child}</div>
+        ),
+        inner,
+      );
+
+      return (
+        <div
+          key={`shape-chrome-${element.id}`}
+          className={CLASSES.SHAPE_CHROME}
+          style={{
+            position: "absolute",
+            transform: `translate(${x - this.state.offsetLeft}px, ${
+              y - this.state.offsetTop
+            }px) scale(${scale})`,
+            transformOrigin: "top left",
+            pointerEvents: POINTER_EVENTS.disabled,
+            zIndex: 3,
+          }}
+        >
+          {clippedInner}
+        </div>
+      );
+    });
+  }
+
   private toggleOverscrollBehavior = (event: React.PointerEvent) => {
     // when pointer inside editor, disable overscroll behavior to prevent
     // panning to trigger history back/forward on MacOS Chrome
@@ -2705,6 +3068,7 @@ class App extends React.Component<AppProps, AppState> {
                               />
                             )}
                           {this.renderFrameNames()}
+                          {this.renderShapeChrome()}
                           {this.isDefaultUIEnabled() &&
                             this.state.activeLockedId && (
                               <UnlockPopup
diff --git a/packages/excalidraw/css/styles.scss b/packages/excalidraw/css/styles.scss
index 068ba0389..90ad0d9a5 100644
--- a/packages/excalidraw/css/styles.scss
+++ b/packages/excalidraw/css/styles.scss
@@ -997,3 +997,9 @@ body.excalidraw-cursor-resize * {
     font-family: "Assistant";
   }
 }
+
+.excalidraw__shape-chrome,
+.excalidraw__shape-footer {
+  pointer-events: none;
+  z-index: 2;
+}
diff --git a/packages/excalidraw/index.tsx b/packages/excalidraw/index.tsx
index 6bfb0ad6f..a0182544b 100644
--- a/packages/excalidraw/index.tsx
+++ b/packages/excalidraw/index.tsx
@@ -110,6 +110,9 @@ const ExcalidrawBase = (props: ExcalidrawProps) => {
     children,
     validateEmbeddable,
     renderEmbeddable,
+    renderShapeFooter,
+    renderShapeTopLeft,
+    renderShapeTopRight,
     aiEnabled,
     showDeprecatedFonts,
     renderScrollbars,
@@ -254,6 +257,9 @@ const ExcalidrawBase = (props: ExcalidrawProps) => {
           onDuplicate={onDuplicate}
           validateEmbeddable={validateEmbeddable}
           renderEmbeddable={renderEmbeddable}
+          renderShapeFooter={renderShapeFooter}
+          renderShapeTopLeft={renderShapeTopLeft}
+          renderShapeTopRight={renderShapeTopRight}
           aiEnabled={aiEnabled !== false}
           showDeprecatedFonts={showDeprecatedFonts}
           renderScrollbars={renderScrollbars}
diff --git a/packages/excalidraw/types.ts b/packages/excalidraw/types.ts
index 206a192ab..e1a48e2fc 100644
--- a/packages/excalidraw/types.ts
+++ b/packages/excalidraw/types.ts
@@ -38,6 +38,8 @@ import type {
   StrokeVariability,
   ElementShadow,
   NonDeletedSceneElementsMap,
+  FrameAutoLayoutAlign,
+  FrameAutoLayoutHug,
 } from "@excalidraw/element/types";
 
 import type {
@@ -46,10 +48,6 @@ import type {
   ValueOf,
   MakeBrand,
 } from "@excalidraw/common/utility-types";
-import type {
-  FrameAutoLayoutAlign,
-  FrameAutoLayoutHug,
-} from "@excalidraw/element/types";
 
 import type {
   CaptureUpdateActionType,
@@ -983,6 +981,45 @@ export interface ExcalidrawProps {
     element: NonDeleted<ExcalidrawEmbeddableElement>,
     appState: AppState,
   ) => JSX.Element | null;
+  /**
+   * Render a footer overlay inside a shape or frame (rectangle, diamond,
+   * ellipse, or frame). Return `null` to skip an element.
+   *
+   * Positioned at the bottom-center of the element with 16px top/side and
+   * 12px bottom padding. Children are kept above this footer band.
+   * The overlay scales and rotates with the element.
+   * The wrapper has `pointer-events: none`; set `pointer-events: auto` on
+   * your node if it should be interactive.
+   */
+  renderShapeFooter?: (
+    element: NonDeletedExcalidrawElement,
+    appState: AppState,
+  ) => React.ReactNode;
+  /**
+   * Render a top-left overlay inside a shape or frame. Return `null` to skip.
+   *
+   * Positioned at the top-left with 16px side and 12px top padding (same as
+   * the in-frame title). When present on a frame, the native name shifts
+   * right to sit beside this node.
+   * The wrapper has `pointer-events: none`; set `pointer-events: auto` on
+   * your node if it should be interactive.
+   */
+  renderShapeTopLeft?: (
+    element: NonDeletedExcalidrawElement,
+    appState: AppState,
+  ) => React.ReactNode;
+  /**
+   * Render a top-right overlay inside a shape or frame. Return `null` to skip.
+   *
+   * Positioned at the top-right with 16px side and 12px top padding.
+   * Frame children are kept below this header band.
+   * The wrapper has `pointer-events: none`; set `pointer-events: auto` on
+   * your node if it should be interactive.
+   */
+  renderShapeTopRight?: (
+    element: NonDeletedExcalidrawElement,
+    appState: AppState,
+  ) => React.ReactNode;
   aiEnabled?: boolean;
   showDeprecatedFonts?: boolean;
   hideDefaultRotateHandle?: boolean;
